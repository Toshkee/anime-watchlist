<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Update Anime</title>
  <link rel="stylesheet" href="/css/style.css"> <!-- or your shared CSS -->
</head>
<body>
  <%- include('partials/navbar'); %>

  <div class="add-anime-container">
    <h1>Update "<%= anime.title %>"</h1>

    <form method="POST" action="/update/<%= anime._id %>">
      <label style="display:block; text-align:left; margin-bottom:8px;">
        Status
      </label>
      <select name="status" required>
        <option value="Watching" <%= anime.status === 'Watching' ? 'selected' : '' %>>Watching</option>
        <option value="Plan to Watch" <%= anime.status === 'Plan to Watch' ? 'selected' : '' %>>Plan to Watch</option>
        <option value="Completed" <%= anime.status === 'Completed' ? 'selected' : '' %>>Completed</option>
        <option value="Dropped" <%= anime.status === 'Dropped' ? 'selected' : '' %>>Dropped</option>
      </select>

      <label style="display:block; text-align:left; margin:12px 0 6px 0;">
        Notes
      </label>
      <textarea name="notes" placeholder="Notes (optional)"><%= anime.notes %></textarea>

      <button type="submit" style="margin-top:12px;">Save</button>
      <a href="/watchlist" style="display:inline-block;margin-left:10px;color:#ccc;">Cancel</a>
    </form>
  </div>
</body>
</html>